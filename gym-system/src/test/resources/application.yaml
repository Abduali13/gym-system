spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
  security:
    enabled: false
    jwt:
        secret: testSecretKeyForJWT
        expiration-ms: 60000 # 1 minute
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
